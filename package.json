{
  "name": "arcanoid-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Vitest harness for the Arcanoid game JS modules.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
